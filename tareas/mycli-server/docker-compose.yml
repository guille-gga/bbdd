version: '3.8'

services:
  mysql:
    build: .
    container_name: mysql_mycli_server
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  mysql_data: