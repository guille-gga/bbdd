services:
  mysql:
    build: .
    container_name: mysql_mycli_server
    ports:
      - "3356:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  mysql_data:
